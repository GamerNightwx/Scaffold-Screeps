{
  "name": "screeps-bot",
  "version": "0.0.1",
  "private": true,
  "description": "Screeps bot for World, Season, and private servers.",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/main.js --bundle --platform=node --format=cjs --outfile=dist/main.js",
    "check": "node --check src/main.js && node --check dist/main.js",
    "test": "npm run check"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
